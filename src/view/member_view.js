define([

	'jquery',
	'underscore',
	'backbone',
	'model/auth_db',
	'firebase',
	'avatars',
	], function($, _, Backbone, authModel) {
		var memberView = Backbone.View.extend({



		});

		return memberView;
	})